language: node_js
node_js:
- '11'
branches:
  only:
  - hexo
before_install:
install:
- npm install hexo-cli -g
- npm install
script:
- npm run deploy
after_success:
- cd ./public
- echo "www.funguykawayi.cn" >> CNAME
- git init
- git config  user.name "klis"
- git config  user.email "qiang.liao@outlook.com"
- git add --all .
- git commit -m "Travis CI Auto Builder"
# 这里的 REPO_TOKEN 即之前在 travis 项目的环境变量里添加的
- git push --quiet --force "https://${GH_TOKEN}@github.com/hapihapidoge/hapihapidoge.github.io.git" master:master
